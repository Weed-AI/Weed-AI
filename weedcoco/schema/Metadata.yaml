$id: https://weedid.sydney.edu.au/schema/Metadata.json
# Designed to be conformant to flattened schema.org/Dataset
type: object
required:
  - name
  - creator
  - datePublished
  - license
definitions:
  - Person:
    type: object
    required:
      - name
    properties:
      name:
        type: string
      givenName:
        type: string
      familyName:
        type: string
      sameAs:
        type: string
        format: uri
        label: ORCID ID
        description: ORCID ID (see https://orcid.org)
  - Organization:
    type: object
    required:
      - name
    properties:
      name:
        type: string
      sameAs:
        type: string
        format: uri
        label: ROR ID
        description: ROR ID (see https://ror.org)
properties:
  name:
    type: string
    description: A name for this dataset, or its publication title
  creator:
    type: array
    items:
      minItems: 1
      oneOf:
        - $ref: "#/definitions/Person"
        - $ref: "#/definitions/Organization"
    description: People and organizations who created this dataset.
  description:
    type: string
    description: A description of this dataset
  identfier:
    type: array
    items:
      type: string
      format: uri
      description: Digital Object Identifier or other URL established as identifiers for this dataset.
    label: Identifiers (including DOIs)
  sameAs:
    type: array
    items:
      type: string
      format: uri
      description: "Another URL for this dataset"
    label: Other URLs
  datePublished:
    type: string
    format: date
    description: Date of first publication
  citation:
    type: string
    description: Identifies academic articles that are recommended by the data provider be cited in addition to the dataset itself.
  funder:
    type: array
    items:
      $ref: "#/definitions/Organization"
    description: People and organizations who created this dataset.
  license:
    type: string
    format: uri
